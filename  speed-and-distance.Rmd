---
title: "speed-and-distance"
author: "Franco"
date: "2024-06-18"
output: html_document
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

# Define the custom knit function in the setup chunk
knit <- function(input_file, encoding) {
  # Set the output directory
  out_dir <- 'docs'
  
  # Create the full path for the output directory
  out_dir_path <- file.path(getwd(), out_dir)
  
  # Check if the directory exists, if not create it
  if (!dir.exists(out_dir_path)) {
    dir.create(out_dir_path)
  }
  
  # Render the R Markdown file to the output directory
  rmarkdown::render(input_file,
                    encoding = encoding,
                    output_file = file.path(out_dir_path, 'index.html'))
}





```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
